---
- name: Install & Delete 
  hosts: ubuntu-VMs
  become: yes 
  tasks: 
    - name: Install & Delete Loop
      apt:
        name: "{{ item }}"
        state: present
        update_cache: yes
      loop:
        - vim
        - net-tools
        - nano
